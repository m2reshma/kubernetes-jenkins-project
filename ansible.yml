---
- hosts: node
  become: true
  tasks:
    - name: Create new deployment
      command: kubectl apply -f /home/paul/deployment.yml
    - name: Create new service
      command: kubectl apply -f /home/paul/service.yml
